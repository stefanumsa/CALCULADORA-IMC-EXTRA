html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Calculadora IMC - Sistema Métrico
    link(rel="stylesheet", href="/css/style.css")
  body
    nav
      ul
        li
          a(href='/') Pagina Principal
        li
          a(href='/calculadora-imc2') Sistema Inglés
        li
          a(href='/informacion') Informacion
    .container
      h1 Calculadora de IMC
    .calculadora-container
      form(action="/api/bmi" method="POST")
        input(type="hidden" name="unit" value="metrico")
        label(for="height") Altura (metros):
        input(type="number" step="0.01" name="height" required)
        label(for="weight") Peso (kilogramos):
        input(type="number" name="weight" required)
        button(type="submit" class="btn-return") Calcular IMC
